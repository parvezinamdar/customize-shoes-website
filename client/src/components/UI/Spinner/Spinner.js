import React from 'react';
import './Spinner.css';

const spinner = () => (
    <div style={{ height:"100vh"}}>
        <div className="loader">Loading...</div>
    </div>
    
);

export default spinner;